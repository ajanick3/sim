node_modules/
.next/
next-env.d.ts
out/
*.tsbuildinfo

# Copied from the repository by the `assets` script; not source.
public/cards.json
public/decks/

# `npm run wasm` rebuilds this from crates/sim-wasm. It is committed so a
# deploy needs no Rust toolchain; regenerate it after an engine change.
!public/pkg/
